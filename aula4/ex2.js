const numeros = [2, 4, 6, 8, 10];

const dobro = numeros.map(numero => numero * 2);

console.log("Original:", numeros);
console.log("Novo:", dobro);
Ex3:

const precos = [50, 80, 100, 150];

const descontos = precos.map(preco => preco * 0.9);

console.log("Preços originais:", precos);
console.log("Com desconto:", descontos);
